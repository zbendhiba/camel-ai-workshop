- route:
    id: hello-camel
    from:
      uri: timer:hello
      parameters:
        period: 5000
      steps:
        - setBody:
            constant: "Hello from Apache Camel!"
        - log:
            message: "${body}"
